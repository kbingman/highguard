%h1
  Ships controller, index action

%table
  %tr
  - @ships.each do |ship|
    %tr
      %td
        = link_to ship.name, url(:edit_ship,  ship)
      %td
        = link_to 'Show', url(:ship,  ship)

= link_to 'New', url(:new_ship)