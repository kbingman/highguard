%h1
  Ships controller, index action

%table
  %thead
    %tr
      %th Class Name
      %th.tonnage Tonnage
      %th Drives
      %th{ :colspan => "2" } Modify
  %tbody
    - @ships.each do |ship|
      %tr
        %td
          = link_to ship.name, url(:ship, ship)
        %td.tonnage= ship.tonnage
        %td= "J#{ship.jumpdrive} M#{ship.thrust} P#{ship.power}"
        %td
          = link_to 'edit', url(:edit_ship, ship)
        %td
          = link_to "delete", url(:delete_ship, ship), :class => 'delete', :method => 'delete'

= link_to 'New', url(:new_ship)